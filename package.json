{
  "name": "ai-live-translator",
  "version": "0.2.3",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check.mjs",
    "build": "node scripts/vendor.mjs && npm run check",
    "serve": "python3 -m http.server 8080 --directory dist"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.116.0"
  }
}
